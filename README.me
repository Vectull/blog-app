Blog App — Livewire + Alpine + Quill (Reference Project)

Эталонный Laravel-проект, демонстрирующий связку Laravel 12 + Livewire + Alpine.js + Quill.js, с модальными окнами, асинхронной навигацией и сборкой через Vite.

Проект можно использовать как reference / starter для построения SPA-подобных интерфейсов на Livewire без Vue/React.


🔧 Стек технологий

Laravel 12

Livewire 3

Alpine.js — управление UI-состояниями

Quill.js — WYSIWYG-редактор

Vite — сборка JS и CSS

Bootstrap 5 (можно заменить на Tailwind)




Реализованный функционал

Список постов (Livewire component)

Карточка поста (Livewire component)

Модальное окно поста (Livewire component)

Асинхронное открытие/закрытие модалки

Синхронизация URL:

/posts — список

/posts/{id} — список + открытая модалка

Inline-редактирование поста через Quill

Сохранение с форматированием (HTML)

Архитектура по принципам DRY / KISS / SOLID


📁 Архитектура проекта
Livewire-компоненты

app/Livewire/
├── PostsList.php // список постов
├── PostCard.php // карточка поста
└── PostModal.php // модальное окно + редактор

Blade-шаблоны

resources/views/livewire/
├── posts-list.blade.php
├── post-card.blade.php
└── post-modal.blade.php


Frontend (Vite)


resources/js/
├── app.js // точка входа
└── post-modal.js // Alpine + Quill логика


resources/css/
└── app.css


🚀 Установка и запуск
1. Клонирование
git clone https://github.com/Vectull/blog-app.git

cd blog-app

2. Установка зависимостей

composer install

npm install

3. Настройка окружения

cp .env.example .env

php artisan key:generate

Настрой .env (БД и т.д.)

4. Миграции и сиды
php artisan migrate --seed


5. Сборка фронтенда

Для разработки:

npm run dev

Для продакшена:

npm run build
